flow:

Dummysubscirber:
we fetch each topic from 'topic subscriptio config' table,set MQTT 'topic' to subscriber
client gets json format data, save in dict formatas pickle file for eveyr 15mins in a "specific folder"
here we use, ITCdataingestion file as a pipeline

>we keep on monitoring a "specific_folder" where pickle files get stored
transform:
>using the "new_pickle_file"path , we will load rb of data(id,st,ts,vr)
 convert to string with col names as: "sensor_device_id", "parameter_node", "unix_ts", "ts", "raw_data", "json_payload
>meger the data to "device data" and extract_and_insert_grammage -it takes parameters node

ingest:
csv to gmiiot_machine_datacomits once and delets csv

injected to gmiiot_machine_data and machine_grammage_summary


